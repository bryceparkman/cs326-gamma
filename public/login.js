document.getElementById('login').addEventListener('click', async() => {
    //get login profile data
    //validate if username matches password
        //if it does load apartment overview
    //else
        //alert that they don't match
});